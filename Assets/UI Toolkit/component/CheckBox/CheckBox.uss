#CheckBox {
    justify-content: flex-start;
    align-items: center;
    width: 100%;
    flex-direction: row;
    justify-content: flex-start;
    height: 40px;
}

#CheckBoxItem {
    flex-direction: row;
    justify-content: flex-start;
    align-items: center;
    margin-left: 0;
    width: 160px;
}

#CheckBoxItem:hover {
    opacity: 0.85;
}

#CheckBoxItem.checked > #Btn > #Round > #Point {
    display: flex;
    width: 70%;
    height: 70%;
    left: 15%;
    top: 15%;
}

#CheckBoxItem.checked > #Label {
    font-style: bold;
}

#CheckBoxItem > #Btn {
    min-width: auto;
    padding: 5px;
    min-height: auto;
    height: 20px;
    width: 20px;
    padding-top: 0;
    padding-right: 0;
    padding-bottom: 0;
    padding-left: 0;
}

#CheckBoxItem > #Btn > #Round {
    border-width: 5px;
    border-color: var(--default-color);
    background-color: transparent;
    width: 100%;
    height: 100%;
    position: relative;
    border-top-width: 2px;
    border-right-width: 2px;
    border-bottom-width: 2px;
    border-left-width: 2px;
}

#CheckBoxItem > #Btn > #Round > #Point {
    display: flex;
    background-color: var(--default-color);
    width: 0;
    height: 0;
    position: absolute;
    left: 50%;
    top: 50%;
    transition: 0.3s all;
}

#CheckBoxItem > #Label {
    font-size: 14px;
    margin: 0;
    padding: 0;
    margin-left: 10px;
    color: var(--default-color);
    -unity-font: resource('Fonts/AlimamaShuHeiTi-Bold');
}

#CheckBoxItem.primary > #Btn > #Round {
    border-color: var(--primary-color);
}

#CheckBoxItem.primary > #Btn > #Round > #Point {
    background-color: var(--primary-color);
}

#CheckBoxItem.primary > #Label {
    color: var(--primary-color);
}

#CheckBoxItem.info > #Btn > #Round {
    border-color: var(--info-color);
}

#CheckBoxItem.info > #Btn > #Round > #Point {
    background-color: var(--info-color);
}

#CheckBoxItem.info > #Label {
    color: var(--info-color);
}

#CheckBoxItem.warn > #Btn > #Round {
    border-color: var(--warn-color);
}

#CheckBoxItem.warn > #Btn > #Round > #Point {
    background-color: var(--warn-color);
}

#CheckBoxItem.warn > #Label {
    color: var(--warn-color);
}

#CheckBoxItem.disabled > #Btn > #Round {
    border-color: var(--disabled-color);
}

#CheckBoxItem.disabled > #Btn > #Round > #Point {
    background-color: var(--disabled-color);
}

#CheckBoxItem.disabled > #Label {
    color: var(--disabled-color);
}
